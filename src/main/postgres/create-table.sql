CREATE TABLE odberove_miesto (
    om_id                     VARCHAR(255),
    om_name                   VARCHAR(1023),
    actual_waiting_number     VARCHAR(255),
    last_update               VARCHAR(255),
    db_insert_time            TIMESTAMP
);